import 'package:flutter/material.dart';
class BoardingPassBodyWidget extends StatelessWidget {
  const BoardingPassBodyWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [
          Row(
            children:[]
          ),
          Row(
              children:[]
          ),
          Row(
              children:[]
          ),
          Row(
              children:[]
          ),
        ],
      ),
    );
  }
}
